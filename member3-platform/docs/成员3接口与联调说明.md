# 成员3接口与联调说明

## 运行模式

系统支持**链下/链上双模**（`web3.enabled` 开关），已对接成员1的
`RWA_Core_Asset.sol`（ERC-3525，仓库：merlin746/supply-chain-rwa-platform）：

- **链下存证模式**：全流程落 MySQL，`tx_hash` 以 `OFFCHAIN-` 前缀占位，无需链节点即可演示。
- **链上模式**：
  - 凭证开立 → `mintRWAAsset(to, slot, value, maturityDate, contractHash, uri)`（核心企业私钥签名）
  - 凭证拆分 → `transferFrom(fromTokenId, to, value)`（持有方私钥签名，合约铸造继承 Slot 的子凭证）
  - 一键放款 → 尝试 `settleAsset(tokenId)`（银行私钥；合约要求已到期，未到期 revert 时仅落库清算）
  - 银行核验 → 追加 `balanceOf` / `ownerOf` 链上交叉校验，比对链上余额与业务库
  - 事件同步 → 每 10 秒解码链上事件回写业务库（见下）

约定：
- 金额单位：链上 uint256 为「分」，库中为「元」（×100，`ChainTxService.toCents/toYuan`）
- 私钥托管：`web3.keys.<企业ID>`，平台代各企业签名；本地演示预置 Hardhat 账户 #1~#5 公开私钥
- 链上交易失败回退：`web3.tx-fallback-offchain`（默认 true，演示容错；生产建议 false）

## 已对接的链上事件（成员1合约）

| 事件 | 处理 |
| --- | --- |
| AssetCreated(tokenId, issuer, slot, faceValue, maturityDate, contractHash, metadataURI) | 补缺入库，ownerOf 反查持有方映射企业 |
| AssetSplit(parentTokenId, childTokenId, value) | 补缺（后端自身拆分已落库则跳过，幂等） |
| AssetStatusChanged(tokenId, old, new) | 1→FROZEN 2→REVOKED 3→SETTLED（0 Active 不覆盖业务状态） |
| AssetSettled(tokenId, amount) | 凭证 → SETTLED |
| AssetRevoked(tokenId, revokedBy, reason) | 凭证 → REVOKED |
| TransferValue / Transfer / RoleGranted 等 | 原始日志存入 blockchain_event，不改业务表 |

## 后端接口

### 系统
- GET /api/health
- GET /api/chain/status （返回 `mode: ONCHAIN | OFFCHAIN`）

### 登录（演示级，明文密码）
- POST /api/auth/login  `{username, password}`

演示账号（密码均为 123456）：
`byd`（比亚迪/核心企业）、`kdl`（科达利）、`jnyt`（聚能永拓）、`cytf`（长园特发）、`ccb`（建设银行）

### Dashboard
- GET /api/dashboard/overview

### 企业与发票
- GET /api/enterprise/list
- GET /api/enterprise/{id}
- GET /api/enterprise/invoices?supplierId=
- POST /api/enterprise/invoice
- PUT /api/enterprise/invoice/{id}/audit  `{action: "APPROVE" | "REJECT"}`
- GET /api/enterprise/tokens

### RWA 凭证
- GET /api/token/list?enterpriseId=&status=&ownerAddress=
- GET /api/token/{tokenId}
- POST /api/token/mint  `{invoiceId}` — 基于已审核发票开立凭证，占用核心企业授信
- POST /api/token/split  `{fromTokenId, toEnterpriseId, value}` — 无损拆分（金额=余额时退化为整体转让）
- GET /api/token/topology — 信用穿透拓扑（含确权/拆分/融资三类边）
- GET /api/token/{tokenId}/verify — 溯源核验，回溯至根凭证并返回关联发票与融资记录

### 融资
- GET /api/loan/list?status=
- POST /api/loan/apply
- PUT /api/loan/{id}/approve — 审批通过，凭证转 PLEDGED
- PUT /api/loan/{id}/disburse — 一键放款，凭证转 SETTLED
- PUT /api/loan/{id}/reject

## 凭证状态机

```
UNCIRCULATED --拆分/转让--> CIRCULATING --融资审批--> PLEDGED --放款清算--> SETTLED
```

拆分守恒约束：子凭证金额 + 父凭证剩余金额 == 拆分前父凭证金额；
子凭证继承父凭证 Slot（到期日）与核心企业信用背书（metadata）。

## 成员1交付状态（已完成对接）

- ✅ RWA_Core_Asset.sol 源码（team-repo/contracts/）
- ✅ 编译后 ABI 已装入 `backend/src/main/resources/contracts/RWA_Core_Asset.json`
- ✅ 部署脚本 scripts/deploy.js（自动授角色 + 签发 1000 万演示凭证）
- ✅ 事件定义与参数顺序已确认并对接（见上表）
- 接入真实 FISCO BCOS / Geth 节点时：替换 `web3.rpc-url`、`web3.contract-address`、`web3.keys`

## 成员2需要给成员3（待交付）

1. Circulation 合约地址
2. Settlement 合约地址
3. ABI
4. splitAndTransfer / pledgeForLoan / autoSettle 的参数定义
5. 事件定义（FinancingApplied 等）

## 为什么先保存 blockchain_event

成员1/2合约如果最终 ABI 还没固定，后端不应该自行猜测事件参数。
因此第一阶段：
- topic0
- topics
- data
- txHash
- blockNumber
- contractAddress

全部保留。

ABI 固定后再做精确参数解码，写入 rwa_token / loan_application。

这样可以避免因为合约最终修改导致数据库脏数据。

## 数据库导入注意（Windows）

MySQL 命令行客户端默认按 GBK 解释脚本，导入中文种子数据会报
`ERROR 1406 Data too long`。请加字符集参数：

```bash
mysql --default-character-set=utf8mb4 -uroot -p < database/rwa_supply_chain.sql
```
